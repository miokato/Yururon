# Cloudflare Pages 設定. `npm run build` で生成した `dist/` を配信する.
# カスタムドメイン (apex `yururon.app`) は Pages プロジェクト側で設定する.
name = "yururon-docs"
pages_build_output_dir = "dist"
compatibility_date = "2026-05-01"
